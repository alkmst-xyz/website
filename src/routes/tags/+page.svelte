<h1>All Tags</h1>

<div>
	<!-- TODO -->
	<!-- {% for tag in collections.tagList %}
    {% set tagUrl %}/tags/{{ tag | slugify }}/{% endset %}
    <a href="{{ tagUrl | url }}">{{ tag }}</a>
  {% endfor %} -->
</div>
